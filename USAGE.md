# 关节模组监控台使用说明

本文面向使用关节模组监控台（`joint_gui`）进行教学与演示的学生和教师，介绍界面布局、操作流程、各控件含义与安全注意事项。程序部署与安装见 [`DEPLOY.md`](DEPLOY.md)。

## 1. 简介

关节模组监控台是一个 Qt 图形界面，用于对关节模组（伺服关节）进行**监控**与**控制**：

- **双总线支持**：EtherCAT、CANopen（力矩位置混合 MIT 模式）
- **仿真模式**：无需任何硬件即可完整演示连接、使能、下发目标、观察曲线、急停全流程
- **自动检测**：启动时自动探测总线上是否有设备，没有则回退到仿真模式
- **安全优先**：急停、使能确认、故障复位二次确认、连接看门狗

界面统一使用物理单位：**位置（deg）、速度（deg/s）、力矩（N·m）**。

## 2. 启动与连接

### 2.1 启动

```bash
cd joint-gui
./run.sh          # 开发机上直接运行（自动设置 SDK 库路径）
```

启动后弹出**连接关节模组**对话框。

### 2.2 连接对话框

| 项目 | 说明 |
|------|------|
| 总线类型 | `自动检测`（默认）、`仿真`、`EtherCAT`、`CANopen` |
| EtherCAT 网卡 | 直连关节的网卡名（用 `ip link` 查看），仅 EtherCAT 模式可编辑 |
| 从站 ID | 关节在总线上的节点号，默认 1 |
| CAN 波特率 | 1000 / 500 / 250 kbps，仅 CANopen 模式可编辑 |
| EtherCAT 周期 (ms) | 控制周期，1~5ms，默认 2 |
| 编码器分辨率 (脉冲/圈) | EtherCAT 模式下换算必需，默认 65536 |
| 减速比 | 电机端到输出端的减速比，默认 1 |
| 额定力矩 (N·m) | 力矩换算基准，默认 1 |

选**自动检测**时，总线相关字段（网卡、波特率、周期）会被禁用，由程序在检测时自动决定。

### 2.3 自动检测顺序

选择"自动检测"并点确定后，程序按以下顺序尝试连接，进度显示在底部状态栏：

1. **EtherCAT**：遍历所有活动的非回环网卡，逐个尝试初始化 EtherCAT 主站并扫描从站
2. **CANopen**：尝试初始化 CAN 适配器并读取从站
3. **仿真**：前两者都没有设备时自动进入仿真模式

无论接到哪种总线，界面操作方式完全相同。无硬件时直接演示仿真模式即可。

## 3. 界面布局

主窗口分为三块：**监控面板**（左）、**控制面板**（右）、**实时曲线**（下）。

```
┌──────────────────────────────────────────────────────────┐
│ 状态栏：●运行/○故障 | 总线 | 从站ID | 刷新率                │
├───────────────────────────┬──────────────────────────────┤
│ 监控面板 MonitorPanel      │ 控制面板 ControlPanel         │
│  位置 (deg)                │  ┌──────── 急停 ESTOP ─────┐  │
│  速度 (deg/s)              │  └────────────────────────┘  │
│  力矩 (N·m)                │  □已确认现场安全 [使能][失能]   │
│  驱动器温度 (°C)           │  [故障复位]  操作模式 ▾        │
│  状态字 (hex)               │  目标: 位置/速度/力矩          │
│  驱动状态                   │  轮廓速度/加速度/减速度         │
│  故障码                     │  MIT KP / KD                 │
│  连接状态 / 刷新率          │  [下发目标] [停止运动]         │
├───────────────────────────┴──────────────────────────────┤
│ 实时曲线：位置(蓝) 速度(绿) 力矩(橙)                        │
└──────────────────────────────────────────────────────────┘
```

### 3.1 监控面板

实时刷新关节状态（控制周期每 2~10ms 更新一次）：

| 显示项 | 含义 |
|--------|------|
| 位置 (deg) | 关节当前角度，两位小数 |
| 速度 (deg/s) | 当前角速度 |
| 力矩 (N·m) | 当前输出力矩，三位小数 |
| 驱动器温度 (°C) | 驱动器温度；CANopen 无温度接口时显示 N/A |
| 状态字 (hex) | CiA402 状态字原始值（16 进制） |
| 驱动状态 | 未就绪 / 禁止合闸 / 待合闸 / 已合闸 / 运行使能 / 快速停机 / 故障反应 / 故障 |
| 故障码 | 故障代码（16 进制）；无故障显示"无" |
| 连接状态 | 在线（绿）/ 离线（红） |
| 刷新率 | 遥测到达界面的实际频率（Hz） |

### 3.2 控制面板

| 控件 | 说明 |
|------|------|
| 急停 ESTOP | 红色醒目按钮。按下立即 `快速停机 + 失能`，并**复位安全确认勾选** |
| 已确认现场安全 | 使能前的安全确认勾选。未勾选时"使能"按钮不可用；急停后需重新勾选 |
| 使能 | 接通驱动器使能。点击时先下发当前操作模式，再使能 |
| 失能 | 断开驱动器使能 |
| 故障复位 | 复位驱动器故障，需在弹出的确认框点"是" |
| 操作模式 | 同步位置 CSP / 同步速度 CSV / 同步力矩 CST / 力矩位置混合。**CANopen 仅支持力矩位置混合**（模式下拉会自动锁定） |
| 目标位置/速度/力矩 | 按所选模式下发对应的目标值 |
| 轮廓速度/加速度/减速度 | 位置模式下定义运动轮廓（deg/s、deg/s²） |
| MIT KP / KD | 力矩位置混合模式的刚度/阻尼系数 |
| 下发目标 | 按当前模式打包并下发目标命令 |
| 停止运动 | 下发速度 0 的目标（速度类模式与仿真下有效；位置模式下的实际停止以硬件表现为准） |

### 3.3 实时曲线

位置（蓝）、速度（绿）、力矩（橙）三条滚动曲线，各自独立自动缩放，便于观察波形变化趋势。

## 4. 基本操作流程

> 以下流程在**仿真模式**下即可完整操作（无需硬件）。

### 4.1 连接并观察
1. 启动程序，连接对话框选"自动检测"（或"仿真"），点确定
2. 状态栏显示已连接总线与从站数；监控面板开始刷新，曲线开始滚动

### 4.2 使能
3. **勾选"已确认现场安全"**（必须）
4. 点 **使能** —— 驱动状态变为"运行使能"

### 4.3 下发目标
5. 选择操作模式（默认 CSP 同步位置）
6. 填目标位置（如 30）与轮廓速度，点 **下发目标**
7. 监控面板位置值逐渐逼近目标，曲线显示运动过程

### 4.4 停止
8. 点 **停止运动** 停止目标运动，或点 **失能** 断开使能

### 4.5 急停与故障恢复
9. 点 **急停 ESTOP** —— 立即快速停机并失能；安全确认勾选被复位
10. 若出现故障：监控面板显示"故障"与故障码，点 **故障复位** → 确认
11. 重新勾选安全确认 → 再次 **使能**

## 5. 操作模式说明

| 模式 | 说明 | 需要设置的目标值 |
|------|------|------------------|
| 同步位置 CSP | 位置闭环，按轮廓运动 | 目标位置、轮廓速度/加速度/减速度 |
| 同步速度 CSV | 速度闭环 | 目标速度 |
| 同步力矩 CST | 力矩开环/闭环 | 目标力矩 |
| 力矩位置混合 MIT | 位置+速度+力矩+刚度/阻尼同时控制（力位混合） | 目标位置、目标速度、目标力矩、MIT KP、MIT KD |

> 模式选择在每次点击"使能"时下发到驱动器。运行中切换模式需重新点击"使能"生效。
> CANopen 设备内部固定为力矩位置混合模式，界面会自动锁定该选项。

## 6. 安全功能

| 功能 | 说明 |
|------|------|
| 急停 | 界面最显眼按钮，一键快速停机 + 失能 |
| 使能确认 | 使能前必须勾选"已确认现场安全" |
| 故障复位确认 | 复位故障需二次确认 |
| 看门狗 | 遥测超过 500ms 未更新即判定连接异常，自动断开并提示 |
| 断连失能 | 断开连接/退出程序时先尽力失能，避免电机残留目标 |

**实机使用提醒**：急停按钮是软件总线命令，不能替代硬件急停回路；在真实设备上操作前请确保现场安全、目标速度/行程在合理范围。

## 7. 常见问题

| 现象 | 处理 |
|------|------|
| 点了使能没反应 | 确认已勾选"已确认现场安全"；看驱动状态是否处于"运行使能"或"故障" |
| 显示"故障" | 记下故障码，点"故障复位"→ 确认；若反复故障请检查接线与参数 |
| 曲线不滚动 | 确认已连接（状态栏显示总线与从站数）；无硬件时应选仿真模式 |
| 自动检测落到仿真但接了设备 | 见 DEPLOY.md：EtherCAT 需 root/CAP_NET_RAW、网卡直连；CANopen 需适配器驱动 |
| 下发目标后不动 | 确认已使能；检查模式与目标值是否匹配（如 CSP 需要目标位置） |
| 力矩位置混合模式不动 | 检查 MIT KP/KD 是否过小（KP=0 时无位置刚度）；目标值需在设备限幅内 |

## 8. 单位约定

界面一律使用：位置 deg、速度 deg/s、力矩 N·m、温度 °C。
- EtherCAT 设备层为脉冲/千分比，由程序按编码器分辨率与减速比自动换算
- CANopen 设备层为 rad 系，由程序自动换算为 deg
